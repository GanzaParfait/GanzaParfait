-- Intro Experience lives in site_settings.settings_json.introExperience
-- (enabled, frequency, colors, timing, greetings[]). No dedicated table.
-- Optional seed applied by application defaults in src/lib/intro-experience.ts.
SELECT 1;
