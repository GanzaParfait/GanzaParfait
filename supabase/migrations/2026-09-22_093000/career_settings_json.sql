-- Career / Experience records live in site_settings.settings_json as `career`
-- (page copy, stats, and records for roles, education, and certifications).
-- Homepage Journey selects which record ids to display via homepage.journey.selectedIds.
-- CV formats continue to select items via cvConfig itemIncludes keys
-- (experience:{id}, education:{id}, certifications:{id}, …).
-- No new tables or columns are required.
SELECT 1;
