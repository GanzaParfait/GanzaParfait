-- CV / Resume configuration is stored inside site_settings.settings_json as `cvConfig`.
-- No new columns or tables are required. Saving from Control Center → CV / Resume
-- persists formats, public default, section toggles, and CV-only text overrides.
-- Public surface: /cv and GET /api/cv/pdf?template=professional|compact|executive
SELECT 1;
