-- CV / Resume configuration lives in site_settings.settings_json as `cvConfig`
-- (formats, headlines, section order, record selection, languages, expertise).
-- Revision 2 adds languages, core expertise, and format-specific IA.
-- No new tables or columns are required.
SELECT 1;
